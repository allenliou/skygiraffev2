testing redirect  It is currently [% timest %]

<br>
room is [% roomName %]
roomid is [% roomId.$room_id %]
<br>
or roomid is [% roomId.$room_id.room_id %]
roomid is [% roomId.room_id.room_id %]

    [% FOREACH i IN timeHash.keys.nsort %]
    <p>[% timeHash.$i %]</p>
    [% END %]
